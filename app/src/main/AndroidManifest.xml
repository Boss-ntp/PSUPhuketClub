<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.psuclubapp.psuclubapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        //Admin
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //act
        <activity
            android:name=".Main2Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditClubActivity" />
        <activity android:name=".RegisAppActivity" />
        <activity android:name=".ShowDetailClubActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".ClubHeadActivity" />
        <activity android:name=".ClubMeActivity" />
        <activity android:name=".ClubFollowActivity" />
        <activity android:name=".ClubAllActivity" />
        <activity android:name=".EditActActivity" />
        <activity android:name=".ShowDetailActActivity" />
        <activity android:name=".ActFollowActivity" />
        <activity android:name=".ActHeadActivity" />
        <activity android:name=".ActAllActivity" />
        <activity android:name=".ActMeActivity"></activity>
    </application>

</manifest>